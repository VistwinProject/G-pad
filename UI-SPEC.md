# UI 規格書 — 火災黃金30秒 / 雲端宅邸

這份文件是**從實際畫面量出來的**（不是抄 CSS），涵蓋五個畫面上所有的文字、字體、字級、字重、字距和顏色。

- 量測基準：**1920 × 1080**，`rem = 16px`
- 量測日期：2026-09-03
- 量測方式：瀏覽器裡對每個含文字的元素讀 `getComputedStyle`

---

## 0. 全域

### 0.1 字體

| 用途 | 字體 | 載入的字重 |
| --- | --- | --- |
| 中文、一般文字 | **Noto Sans TC** | 400 / 500 / 700 / 900 |
| 數字、英文標籤 | **Oswald** | 300 / 400 / 500 / 600 / 700 |

完整 fallback：`"Noto Sans TC", "PingFang TC", "Microsoft JhengHei", system-ui, sans-serif`
（Google Fonts CDN 載入，離線環境要自己備字型檔）

### 0.2 rem 的算法

```css
html{ font-size:max(8px, min(.8333vw, 1.4814vh)) }   /* 1920×1080 時 = 16px */
```

**整個版面是等比縮放的** —— 取寬高較小者換算，所以 4:3 或直式螢幕會自動縮小。
下面所有字級都以 `rem` 為準，px 只是 1920×1080 下的換算值。

⚠️ Pad **不吃這一條**（而且程式在另一個 repo：VistwinProject/G-pad）。見 §5。

### 0.3 ⚠️ 編輯模式的縮放會再乘一次

四個看板頁的每個區塊都是可拖曳縮放的物件（按 `E` 進編輯模式），縮放值存在 `layout*.json`。
**畫面上實際看到的字級 = 下表的 rem × 該物件的 scale。**

| 頁 | 物件 | scale |
| --- | --- | --- |
| 第一頁 | hero 1.609 / timerRing 1.663 / timerFoot 2.409 / sideHead 2.547 / phase1~3 ≈1.58 / act1~4 1.30 / warn 1.204 / stage 1.038 / actsTitle 1 / sideTag 1 |  |
| 首頁 | homeBrand 1.55 / homeTagline 3.53 / homeSub 1.081 / homeStatus 1.599 / homeGuideTitle 2.183 / homeAct1~3 1.2 / homeStage 0.904 / 兩顆按鈕 1 |  |
| 前言＋結語（共用一份） | introEyebrow 0.786 / introRoll 0.822 / introPlan 1.887 / introLogo 0.91 |  |

例：首頁的 `.home__sub` CSS 是 `0.454rem`（7.3px），但它在 `homeTagline` 裡、外框放大 3.53 倍，
所以螢幕上大約是 **26px**。

### 0.4 色票（`:root`，藍色主題）

| 變數 | 值 | 用在哪 |
| --- | --- | --- |
| `--bg-0` | `#020912` | 最深的底 |
| `--bg-1` | `#061527` | 漸層中段 |
| `--bg-2` | `#0a2038` | 漸層亮段 |
| `--brand` | `#2f9dff` | 主色藍 |
| `--brand-hi` | `#74d4ff` | 亮藍（標題、數字、重點字） |
| `--brand-line` | `rgba(47,157,255,.22)` | 框線 |
| `--brand-soft` | `rgba(47,157,255,.13)` | 光暈 |
| `--ink` | `#eaf6ff` | 主要文字 |
| `--ink-2` | `rgba(213,238,255,.66)` | 次要文字 |
| `--ink-3` | `rgba(178,213,245,.38)` | 說明文字 |
| `--red` | `#ff4a3d` | 警示 / 起火點 |
| `--amber` | `#ffb42e` | 注意 / 警語 |
| `--exit` | `#3dffa0` | 安全出口（**語意色，綠色主題下也不變**） |
| `--stage-0/1/2` | `#74d4ff` / `#ffb42e` / `#ff4a3d` | 倒數三個時段 |
| `--m-wall` | `#123049` | 3D 牆 |
| `--m-slab` | `#e4efff` | 3D 樓板 |
| `--m-edge` | `#74d4ff` | 3D 框線 |
| `--m-fog` | `#031020` | 3D 霧 |
| `--m-floor` | `#0a1c30` | 3D 地面 |
| `--m-shade` | `#4d6a86` | 通關變白時的陰影色 |
| `--ease` | `cubic-bezier(.22,.61,.36,1)` | 全站緩動 |

另有綠色主題 `:root[data-theme="green"]`，以及紅（`red`）／綠（`green`）兩個情境配色，
切換時 `--brand` 系列整組換掉，`--exit` / `--red` / `--amber` 這些語意色不變。

---

## 1. 前言頁（按 `A`）

四角 HUD 框角 + 底部刻度 + ANLB 字標。三段文字**疊在同一個位置往上滾動輪替**，每段 5 秒。

| 元素 | 文字 | 字體 | 字級 | 字重 | 字距 | 顏色 |
| --- | --- | --- | --- | --- | --- | --- |
| `.intro__chip` | 前言 · INTRO | Noto Sans TC | 1.2rem / 19.2px | 500 | .26em | `--brand-hi` |
| `.intro__title` 左段 | 意外發生 | Noto Sans TC | 3.57rem / 57.1px | 700 | .086em | `--brand-hi` |
| `.intro__title` `<em>` | 每一秒 | Noto Sans TC | **5.1rem / 81.6px** | 700 | .06em | `--brand-hi` |
| `.intro__title` 右段 | 都是關鍵 | Noto Sans TC | 3.57rem / 57.1px | 700 | .086em | `--brand-hi` |
| `.intro__lead` | 從即時感知到智慧引導，讓安全更早一步。 | Noto Sans TC | 1.864rem / 29.8px | 400 | .08em | `--ink-2` |
| `.intro__sign` | **寶舖**讓每一秒，都多一分安心。 | Noto Sans TC | 1.791rem / 28.7px | 400 | .55em | `--brand-hi` 70% |

- 主標行高 1.3，三段**對齊基線**；左右兩段是 `font-size:.7em`（相對中間那段）
- 輪播三段**底部對齊**（`align-items:flex-end`）
- ⚠️ 副標和結語的字級被 `--lead-k:.981` / `--sign-k:1.3775` 補償過 —— 三段本來是各自獨立的物件、
  各有各的縮放，併成一個輪播框之後共用外框的 0.822，這兩個係數把原本看到的大小補回來
- ANLB 字標：`assets/anlb-neon.png`，`opacity .6`，框寬 `14.4rem`（字標本身約是框寬的 90.7%）

---

## 2. 火災黃金30秒（按 `Esc`，開機第二頁）

| 元素 | 文字 | 字體 | 字級 | 字重 | 字距 | 顏色 |
| --- | --- | --- | --- | --- | --- | --- |
| `.hero__zh` | 火災黃金**30**秒 | Noto Sans TC | 3.5rem / 56px | 700 | .02em | `--ink` |
| `.hero__num` | 30（在 `.hero__zh` 裡） | **Oswald** | 5.67rem / 90.7px | 600 | −.01em | `--brand-hi` |
| `.hero__en` | THE GOLDEN 30 SECONDS | **Oswald** | 1.35rem / 21.6px | 400 | .30em | `--ink-2` |
| `.hero__tag` | 關鍵時刻，決定生死 | Noto Sans TC | 1.06rem / 17px | 500 | .14em | `--ink-2` |
| `.stage__label` | 3D MODEL / 逃生動線 | **Oswald** | .72rem / 11.5px | 400 | .24em | `--ink-3` |
| `.stage__hint` | 拖曳 **.glb / .gltf** 檔案到此處載入模型 | Noto Sans TC | .72rem / 11.5px | 400 / 500 | .06em | `--ink-3` / `--ink-2` |
| `#stage-progress` | 0%（載入中） | **Oswald** | .9rem / 14.4px | 400 | .20em | `--brand-hi` |
| `.legend li` | 起火點 · 安全出口 | Noto Sans TC | .72rem / 11.5px | 400 | .08em | `--ink-3` |
| `.timer__value` | **30**（倒數數字） | **Oswald** | 8.4rem / 134.4px | 500 | 0 | `--brand-hi`（時段變色） |
| `.timer__value i` | 秒 | Noto Sans TC | 2.016rem / 32.3px | 700 | 0 | 同上 |
| `.timer__caption` | 黃金逃生時間 | Noto Sans TC | .92rem / 14.7px | 400 | .34em | `--brand-hi` |
| `.timer__foot` | 把握 **30** 秒，安全逃生！ | Noto Sans TC | 1.02rem / 16.3px | 500 | .10em | `--ink-2` |
| `.timer__foot b` | 30 | **Oswald** | 1.183rem / 18.9px | 500 | .086em | `--brand-hi` |
| `.acts__title` | 正確應對，提升生存機率 | Noto Sans TC | 1.12rem / 17.9px | 600 | .12em | `--ink` |
| `.act b` ×4 | 立即警報 / 保持冷靜 / 關門阻煙 / 安全集合 | Noto Sans TC | .9rem / 14.4px | 700 | .10em | `--ink` |
| `.act__sub` ×4 | 按下警報器通報 / 循著標示逃生 / 隔絕火勢蔓延 / 等待消防救援 | Noto Sans TC | .7rem / 11.2px | 400 | .04em | `--ink-3` |
| `.side-head h2` | 每一秒都很關鍵 | Noto Sans TC | 1.62rem / 25.9px | 700 | .06em | `--brand-hi`（時段變色） |
| `.side-head p` | 火勢與濃煙擴散極快\<br\>把握逃生時機 | Noto Sans TC | .8rem / 12.8px | 400 | .06em | `--ink-3` |
| `.phase__t` ×3 | 0-10 / 10-20 / 20-30 | **Oswald** | 1.55rem / 24.8px | 500 | .02em | `--brand-hi` |
| `.phase__t i` | 秒 | Noto Sans TC | .899rem / 14.4px | 700 | .034em | `--brand-hi` |
| `.phase__d` ×3 | 火災初期，立即警覺\<br\>尋找最近逃生路線 / 火勢快速擴大\<br\>濃煙開始蔓延 / 能見度下降，吸入濃煙\<br\>易造成嚴重傷害 | Noto Sans TC | .78rem / 12.5px | 400 | .03em | `--ink-3` |
| `.warn__text` | 火災無情，預防先行；設備完善，守護生命 | Noto Sans TC | .92rem / 14.7px | 500 | .20em | `--amber` |

**倒數時段變色**：0-10 秒 `--stage-0` 藍 → 10-20 秒 `--stage-1` 黃 → 20-30 秒 `--stage-2` 紅，
「30」和「每一秒都很關鍵」兩處會跟著換色。

---

## 3. 首頁 / 逃生動線示範（按 `Enter`）

| 元素 | 文字 | 字體 | 字級 | 字重 | 字距 | 顏色 |
| --- | --- | --- | --- | --- | --- | --- |
| `.home__name` | 雲端宅邸 | Noto Sans TC | 3.5rem / 56px | 700 | .06em | `--ink` |
| `.home__sub` | AI 預見風險，安全先行一步 | Noto Sans TC | .454rem / 7.3px | 400 | .06em | `--ink-3` |
| `.stage__label` | CLOUD RESIDENCE / 智慧安全系統 | **Oswald** | .72rem / 11.5px | 400 | .24em | `--ink-3` |
| `.home__tagline b` ×3 | 即時感知 / 動態判斷 / 智慧分流 | Noto Sans TC | .92rem / 14.7px | 500 | .14em | 待機時依序亮紅／黃／綠 |
| `.home__tagline em` | ×（分隔） | Noto Sans TC | .662rem / 10.6px | 500 | .194em | `--brand-hi`，opacity .5 |
| `.home__status b` ×4 | 火源位置 / 煙霧擴散 / A出口 / B出口 | Noto Sans TC | .95rem / 15.2px | 500 | .10em | `--ink` |
| `.home__status i` ×4 | 已辨識 / 低風險 / 壅塞 / 暢通 | Noto Sans TC | .6rem / 9.6px | 500 | .10em | `--red` 62% / `--exit` 62% |
| 按鈕 1 `b` | 展示逃生動線 | Noto Sans TC | 1.2rem / 19.2px | 700 | .16em | `--ink` |
| 按鈕 1 `i` | ROUTE DEMO | **Oswald** | .7rem / 11.2px | 400 | .26em | `--ink-3` |
| 按鈕 2 `b` | 切換逃生動線 | Noto Sans TC | 1.2rem / 19.2px | 700 | .16em | `--ink` |
| 按鈕 2 `i` | SWITCH ROUTE | **Oswald** | .7rem / 11.2px | 400 | .26em | `--ink-3` |
| `.home__clear b` | 恭喜通關 | Noto Sans TC | 3.2rem / 51.2px | 700 | .30em | `--ink` |
| `.home__clear i` | SAFELY EVACUATED | **Oswald** | .9rem / 14.4px | 400 | .30em | `--brand-hi` |
| `.home__act b` ×3 | 立即警報 / 逃生路線 / 安全法規 | Noto Sans TC | .9rem / 14.4px | 700 | .10em | `--ink` |
| `.home__guide-title` | **[新大樓名稱]** 智慧安全指南 | Noto Sans TC | 1.5rem / 24px | 700 | .06em | `--ink`（大樓名 `--brand-hi`） |
| `.home__guide-slogan` | 每一秒都關鍵，安全逃生，守護生命！ | Noto Sans TC | .92rem / 14.7px | 500 | .20em | `--brand-hi` |

**狀態面板會跟著情境變**：

| 情境 | 火源位置 | 煙霧擴散 | A出口 | B出口 |
| --- | --- | --- | --- | --- |
| 待機（藍） | 已辨識（紅） | 低風險（綠） | 壅塞（紅） | 暢通（綠） |
| 跑動線 1/2/3（紅） | 已辨識 | 低風險 | **暢通（綠）** | **壅塞（紅）** |
| 跑動線 4/5（紅） | 已辨識 | 低風險 | 壅塞 | 暢通 |
| 通關（綠） | **已遠離火源（綠）** | 低風險 | **暢通** | **暢通** |

---

## 4. 結語頁（按 `O`）

版面和字級**完全沿用前言頁那一套**（`.intro__*` 是兩頁共用的 class，版面也共用 `layout-intro.json`）。
只有文案不同：

| 元素 | 文字 | 字級 |
| --- | --- | --- |
| `.intro__chip` | 結語 · OUTRO | 1.2rem / 19.2px |
| `.intro__title` 左段 | 預見風險才能 | 3.57rem / 57.1px |
| `.intro__title` `<em>` | **領先** | 5.1rem / 81.6px |
| `.intro__title` 右段 | 危險一步 | 3.57rem / 57.1px |
| `.intro__lead` | 從感知、判斷到引導，讓 AI 守護每一個回家的日常。 | 1.9rem / 30.4px |
| `.intro__sign` | **寶舖**以智慧守護家的每一秒。 | 1.3rem / 20.8px |

字體、字重、字距、顏色都跟前言頁一致（見第 1 節）。
⚠️ 結語的 `.intro__sign` 是 1.3rem、前言是 1.791rem —— 差別來自前言那邊的 `--sign-k` 補償係數。

---

## 5. 住戶端 Pad（`index.html`）

**版面鎖在實機的長寬比**：寬 17.75 : 高 24.97（直向）。視窗比例不對就自動留邊，
所以在筆電瀏覽器上看到的排版跟平板上一模一樣。

**rem 的算法跟看板不一樣**：`clamp(11px, 盒子寬度 × .022, 26px)`。
基準是**盒子的寬度**不是 vmin —— 視窗變寬時 vmin 會跟著變大，但盒子被長寬比綁住沒變大，
字就會撐爆版面。

⚠️ 盒子寬度低於 500px 或高於 1182px 時 rem 會**碰到 clamp 的上下限**，
   版面就不再等比縮放了。實機（820～1024px 寬）落在範圍內，不受影響。

下表的 px 是 **820 × 1180**（盒子 820 × 1153.5、rem = 18.04px）下的實測值。

### 5.1 五種版面

`body[data-view]` 決定版面，`body[data-state]` 決定配色，兩個都由 `js/pad.js` 換。

| 場景（主展示端送來的） | `data-view` | `data-state` | 長什麼樣 |
| --- | --- | --- | --- |
| `intro` | `idle` | `safe` 藍 `--brand-hi` | 乾淨的鎖屏：時間／日期／系統名，不放通知 |
| `golden30` | `alarm` | `alarm` 紅 `--red` | 火災警報：鎖屏轉紅 + 一則警報通知，進場會震動（§5.3） |
| `aiRoute`（還沒挑動線） | `app` | `safe` 藍 | **雲端宅邸 App**：從警報頁向上滑動帶出來（§5.3c） |
| `aiRoute`（跑動線中） | `notice` | `guide` 藍 | 前往 X 出口＋小人（§5.4） |
| `aiRoute`（已通關） | `notice` | `clear` 綠 `--exit` | 同上，整頁轉綠、標題換「已抵達」、小人停在出口 |
| `outro` | `plain` | `clear` 綠 | 燈號 + 大字 |

⚠️ `guide` 是**亮藍 `--brand-hi`**，不是舊版寫的琥珀 —— 逃生指引整頁以亮藍為主，
   琥珀（`--amber`）現在只剩待機頁的太陽圖示在用。

### 5.1b 跟大螢幕對時（動線上的小人）

出口頁的**小人**要跟大螢幕那一個跑在同一個進度上。
做法不是每一格都傳，而是主展示端在狀態裡多送一段：

```
anim : { kind:'count'|'route', t0:<開始時的 Date.now()>, dur:<秒> }
now  : <送出當下的 Date.now()>
```

Pad 收到就自己算 `已跑秒數 = (now - t0)`，之後用 `performance.now()` 往前推。

| ⚠️ | 為什麼 |
| --- | --- |
| 要用 `now - t0`，不能用「本機現在 - t0」 | `t0` 是**大螢幕那台**的時鐘。兩台的系統時間不會一樣，直接相減會差好幾秒甚至好幾分鐘 |
| 主展示端每秒補推一次（心跳） | 轉播站只存最後一份狀態、有人連上就補送。不補推的話，Pad 晚開機／重連拿到的是一份 `now` 很舊的狀態，進度會停在當初推的那一刻 |
| 心跳的 `t0` 不變，Pad 差 < 0.25 秒就不重設基準 | 每秒把時間軸拉一次，畫面上就是每秒抽一下 |
| Pad 除了 rAF 還掛一個 250ms 的計時器 | 瀏覽器在背景／沒有合成／省電模式可能一格 rAF 都不發，只靠 rAF 會整個定住 |

主展示端那邊要動的是 `js/main.js`（`anim` / `pushSync` / 心跳）和 `js/countdown.js`
（`onCycle`，每一輪倒數開始時回報）。**這是跨 repo 的約定，改了兩邊都要動。**

⚠️ `aiRoute` 還沒挑到動線時（`exit` 是 `null`）**絕對不能顯示 `notice`**。
   那時候沒有出口也沒有動線，畫出來的都是假的，現場住戶照著走會出事 ——
   所以改顯示雲端宅邸 App 那一頁（大螢幕此時也正停在首頁待機，語意剛好對上）。

### 5.2 上緣狀態列

待機頁是平板鎖屏那樣的狀態列，其他頁是「住戶端 · RESIDENT ＋ 連線指示」。

| 元素 | 出現在 | 文字 | 字體 | 字級 | 字重 | 字距 | 顏色 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `.pad__brand` | plain / notice | 住戶端 · RESIDENT | **Oswald** | .82rem / 14.8px | 400 | .22em | `--ink-3` |
| `.pad__link b` | plain / notice | 已連線 / 連線中… | Noto Sans TC | .78rem / 14.1px | 700 | .14em | `--ink-2` |
| `.pad__wifi` | idle / alarm | 訊號圖示 | — | 1.35rem / 24.4px 寬 | — | — | **`--ink` 白色**（斷線不換色，改閃爍） |
| `.pad__batt` | idle / alarm | 電量 % | Noto Sans TC | .82rem / 14.8px | 400 | .04em | **`--ink` 白色**（外框、凸點 48% 白，填充實心白） |
| `.pad__foot` | plain / notice | 火災無情 · 預防先行 | Noto Sans TC | .82rem / 14.8px | 400 | .30em | `--ink-3` |

⚠️ 狀態列這兩個圖示一律**白色單色**（都是 `--ink`，只用透明度分層次），跟手機狀態列一樣。

⚠️ 訊號圖示**就是連線指示換一個樣子**，不是裝飾 —— 沒連上大螢幕會**閃爍**（不換色）。
   平板會在這兩頁擺一整天，這是現場唯一看得出「還連著沒有」的地方，不要拿掉那個閃爍。
   （以前是轉紅，但紅色在警報頁的紅底上看不見，而且狀態列要白色單色。）

⚠️ 電量走 Battery Status API，拿不到就維持 HTML 裡的 `100%`。
下緣的標語（`.pad__foot`）在待機頁和警報頁都隱藏 —— 那個位置給鎖屏自己的
「向上滑動以查看」和底部那條。

### 5.3 待機（`idle`）與 火災警報（`alarm`）

**這兩頁是同一個版面**，只有配色和「有沒有那則通知」不同：

- **待機**就是一張**乾淨的鎖屏** —— 鎖頭、時間、日期、系統名，中間留白，下面「向上滑動以查看」
- **警報**是同一張鎖屏轉成紅色，中間跳一則通知出來

⚠️ 待機頁**不要**放常駐的卡片或狀態磚。「平常空著、有事才跳一則」這個對比本身就是資訊，
   待機頁擺東西就把警報的存在感稀釋掉了。（舊版曾經放過通知卡＋三張狀態磚，已移除。）

骨架（`.lock__*`）兩頁共用，量出來完全一樣：鎖頭 65px、時間 101px、日期 178px、
系統名 213px、「向上滑動以查看」686px。警報頁的通知卡在 251px。

| 元素 | 內容 | 字級 | 字重 | 字距 | 顏色 |
| --- | --- | --- | --- | --- | --- |
| `.lock__icon` | 鎖頭 | 1.55rem / 28px 寬 | — | — | `--ink` |
| `.lock__clock` | 時：分 | **5.6rem / 101px** | 300 | .01em | `--ink` |
| `.lock__date` | 9月7日 星期一 | 1.62rem / 29.2px | 500 | .06em | `--ink` |
| `.lock__system` | 安全守護系統 | 1.02rem / 18.4px | 400 | .14em | `--ink` 62% |
| `.lock__more` | 向上滑動以查看 | .98rem / 17.7px | 400 | .16em | `--ink` 55% |
| `.lock__home` | 底部那條 | 8.5rem × .28rem | — | — | `--ink` 62% |

⚠️ 時、分**同一個白**（`--ink`）。舊版把「分」挑成亮藍，跟警報頁對不起來。

⚠️ `.lock__more` 靠 `margin-top:auto` 壓到最下面 —— 待機頁中間是空的，
   要靠它把空白吃掉，不然整組會浮在上半部。

兩頁**共用同一張桌布**（`assets/wallpaper.jpg`，ANLB 的 A，1640×2271、403KB）：

| 頁 | 桌布 | 底色（桌布載不到時的替代） |
| --- | --- | --- |
| 待機 | 原本的藍 | `#0b2144 → #071630 → #030b1a` |
| 警報 | 同一張圖染成紅色 | `#3a0d0e → #1c0507 → #0d0205` |

⚠️ 警報頁**不需要另外一張紅色的圖**：在桌布上疊一層紅色並開 `background-blend-mode:color`，
   那個混色模式取染色層的色相與彩度、保留桌布的明暗，同一張藍圖就變成紅的。
   但**桌布不存在時不要開這一層** —— 它會改去染下面的漸層，整頁變得又亮又平。

⚠️ 兩頁都要一層**上下壓暗**的漸層（中段留亮）。桌布那個 A 的亮線正好穿過時間和
   下緣提示的位置，不壓暗的話白字會疊在亮線上看不清楚。

⚠️ 桌布存成 **jpg 不是 png**：整張幾乎都是平滑漸層，png 存出來 6.1MB、jpg(q92) 只有 403KB，
   而且原圖自帶一層顆粒，剛好打散色階、看不出斷層。1640px 寬 = 版面盒子 820px 的兩倍。

⚠️ 桌布只鋪在待機頁和警報頁（鎖屏那兩頁）。出口頁和結語頁不鋪 ——
   出口頁上面是卡片和平面圖，底下再放一張圖會打架。

#### 警報的呼吸

呼吸的是那則通知的**白色面板本身**，明暗：

```css
body[data-view="alarm"] .alert{ animation:panelBreathe 2.6s ease-in-out infinite }
@keyframes panelBreathe{ 0%,100%{filter:brightness(.80)} 50%{filter:brightness(1.06)} }
```

⚠️ **背景不要跟著呼吸。** 一度做成背景那層紅光在明暗，那不是要的效果 ——
   而且在那張亮的桌布上，淺的範圍根本看不出來、深的範圍又變成整片閃。

⚠️ 用 `filter:brightness` 而不是去調面板底色的透明度：透明度一降，紅色背景就會
   透上來，深色的字對比反而變差。brightness 壓暗時面板從白變淺灰、黑字還是黑字，
   對比不會掉 —— 那張卡上是要照著逃命的資訊，任何亮度下都得看得清楚。

⚠️ 進場的 `alertIn` 和 `panelBreathe` 要**寫在同一個 `animation` 裡並存**。
   `is-buzz` 只寫 alertIn 的話，震動那 0.34 秒會把呼吸整個蓋掉。

⚠️ 要慢、要淺。這是「呼吸」不是「閃爍」—— 整片快閃在平板上很吵，也可能誘發光敏反應
   （所以是 2.6 秒、透明度差 0.4，不是 0.5 秒、0→1）。

⚠️ 這一層是 `::before` 而不是再一層 background，因為 CSS 沒辦法單獨動某一層
   background-image 的透明度。為了讓它墊在**背景之上、文字之下**，
   `body.pad` 要有 `position:relative; z-index:0`（讓 body 自己成為 stacking context），
   這一層才吃得到 `z-index:-1`。少了那兩個屬性它會蓋到文字上面。

⚠️ `prefers-reduced-motion` 要把動畫停掉，但**記得補一個中間值**（`opacity:.42`）——
   不然那層紅光會停在最暗或最亮。

警報頁的紅用專案紅色情境那一組，跟大螢幕跑動線時整個介面轉紅是同一個語彙。

#### 警報頁的通知卡（`.alert`）

| 元素 | 內容 | 字級 | 字重 | 顏色 |
| --- | --- | --- | --- | --- |
| `.alert__badge` | 三角驚嘆號 | 1.15rem 寬 | — | `#e8a01a` |
| `.alert__head b` | 緊急警報 | .98rem / 17.7px | 500 | `#3a3a3c` |
| `.alert__head i` | 現在 | .98rem / 17.7px | 400 | `#8e8e93` |
| `.alert__title` | 住宅火災警報 | 1.42rem / 25.6px | 700 | `#000` |
| `.alert__body` | 系統偵測到建築內發生火災… | 1.06rem / 19.1px | 400 | `#1c1c1e` |
| `.alert__en` | Fire Alert. Evacuate immediately… | .92rem / 16.6px | 400 | `#8e8e93` |

⚠️ 通知卡是**淺色**的（`rgba(243,243,246,.94)` + blur），這是刻意的 ——
   深紅底上跳一張白卡就是手機鎖屏收到通知的樣子。不要為了「配合深色主題」
   把它改成深色，那樣就融進背景裡了。

⚠️ 狀態列只留訊號和電量，靠右。`.pad__stat` 要 `margin-left:auto` ——
   這兩頁左邊的品牌字藏起來了，只靠 `space-between` 會整組跑到左邊去。

#### 警報跳出來的那一下要震

進到警報頁的**那一次**會 `haptic([30, 60, 30, 60, 45])` ——
**觸感回饋那種震動**：短促清脆的三下（每下 30ms 上下，全長 225ms），
不是警報器那種長時間的嗡嗡聲。

視覺上配一個同樣短促的：通知卡彈進來（`alertIn` .34s）＋ 整個版面被輕敲一下
（`padTap` .2s，只有 2px）。

⚠️ 只在**進入**警報頁時震，不能每次 render 都震 —— 主展示端每秒補推一次狀態，
   每次都震的話平板會一直抖個不停。`buzz()` 用 `lastView` 做邊緣觸發。
   「規劃中」的 `aiRoute` 也是 `alarm` 版面，所以從警報頁進到規劃中**不會**再震一次。

⚠️ 那個視覺效果不是裝飾：`navigator.vibrate` 要瀏覽器先拿到使用者互動才生效
   （沒互動過直接回 false，Chrome 還會在 console 印一行），iOS 更是完全沒有這個 API。
   那些情況下它是唯一看得出來的提示。

⚠️ 抖完要把 class 收掉，而且要有**兩條路**：`animationend` 正常會來，但動畫被
   `prefers-reduced-motion` 關掉、或分頁沒在合成時根本不會觸發，class 就永遠留著。
   所以另外掛一個 800ms 的 timeout 兜底。

⚠️ 震動集中在 `js/pad.js` 的 `haptic()` **一個函式**，那是整包唯一碰震動硬體的地方。
   之後包成 App 要換成原生 Haptics 只改那裡。

⚠️ 目前是**進場震一次**，沒有持續重複。要做成一直震，就在 `buzz()` 裡改成 `setInterval`，
   並記得離開警報頁時清掉。

### 5.3c 雲端宅邸 App（`app`）

主展示端切到**首頁（雲端宅邸）、還沒開始跑動線**時的畫面。像手機解鎖進主畫面：
從警報那頁**向上滑動**帶出來（見下）。一旦主展示端挑好動線（送來 `exit`），
就換成「前往 X 出口」那頁。

⚠️ 這一頁的 `data-state` 是 **`safe`**（藍），不是 `alarm` —— 那時候大螢幕也回到待機的
   雲端宅邸首頁，不是在報警。

由上而下：品牌列 → 主視覺（標語＋建物）→ 居家狀態卡 → 三張狀態磚 → 三顆功能 → 頁尾。

| 元素 | 內容 | 字級 | 字重 | 字距 | 顏色 |
| --- | --- | --- | --- | --- | --- |
| `.pad__time` | 狀態列左邊的時間 | .95rem / 17.1px | 400 | .04em | `--ink` |
| `.app__name b` | 雲端宅邸 | 1.62rem / 29.2px | 700 | .10em | `--ink` |
| `.app__name i` | SMART RESIDENCE | **Oswald** .62rem / 11.2px | 400 | .34em | `--ink-3` |
| `.app__title` | 你的安全，由**寶舖守護** | **2.5rem / 45.1px** | 700 | .04em | `--ink`（寶舖守護 `--brand-hi`） |
| `.app__rule` | 全天候智慧安全守護中（兩側細線） | .95rem / 17.1px | 400 | .30em | `--ink-2` |
| `.app__aside` | 更安全的家／更美好的生活 | .78rem / 14.1px | 400 | .16em | `--ink-3` |
| `.app__cardtext b` | 居家狀態正常 | 1.72rem / 31px | 700 | .08em | `--ink` |
| `.app__cardtext i` | AI 安全守護中 | 1.02rem / 18.4px | 400 | .12em | `--ink-2` |
| `.app__slogan` | 每一份安心 · 都是回家的理由 | .92rem / 16.6px | 400 | .20em | `--ink-2` |
| `.atile__label` | 門窗狀態／室內空氣／安全系統 | .95rem / 17.1px | 400 | .08em | `--ink-2` |
| `.atile__value` | 已關閉／正常／待命 | 1.28rem / 23.1px | 700 | .06em | `--brand-hi` |
| `.aact` | 警報紀錄／逃生指引／安全設定 | 1.02rem / 18.4px | 400 | .10em | `--ink`（圖示 `--brand-hi`） |
| `.app__foot b` | 寶舖守護 · 安心生活 | .92rem / 16.6px | 400 | .24em | `--ink-3` |
| `.app__foot i` | SMART RESIDENCE | **Oswald** .62rem / 11.2px | 400 | .34em | `--ink-3` 62% |

主視覺是大樓的藍色線稿：`assets/residence.jpg`（1199×1312、371KB）。
左右**出血到版面邊緣**（`margin:-1.35rem`），上下用 `mask-image` 淡出接進頁面底色。

#### 主視覺那張圖**可以上下滑**（整頁不滑）

原圖幾乎是正方形（0.914），版面的槽位是寬扁的橫帶，所以做成一個**可以滑的窗**：

| 元素 | 是什麼 | 尺寸 |
| --- | --- | --- |
| `.app__pic` | 看得到的**框**，同時是捲動容器 | `aspect-ratio:1200/670`、`max-height:19.9rem`（約 540×236） |
| `.app__picinner` | 裡面的整張圖，照原圖比例 | `aspect-ratio:1199/1312`（約 540×591） |

差出來的約 355px 就是可以滑的範圍 —— 從屋頂花園一路看到低樓層。

⚠️ 會滑的是**這張圖**，不是整頁。`.pad__app` 跟其他四頁一樣「一定要塞進一畫面」，
   不要在它上面加 `overflow` —— 那會變成整頁在捲，是另一回事。

⚠️ 框一定要留 `max-height`。拿掉的話框會照 `aspect-ratio` 長到 485px，
   整頁就塞不下了（這一頁的餘裕接近 0）。

⚠️ 進到這一頁時 js 會把 `scrollTop` 設到可滑範圍的 **22%**（見 `swipe()`）——
   那個位置對到屋頂花園和頂樓那幾層，是最好看的一段。現場是自動輪播，
   上次有人滑到一半的話下次進來要回到這裡。

⚠️ 捲軸要藏（`scrollbar-width:none` ＋ `::-webkit-scrollbar{display:none}`），
   `overscroll-behavior:contain` 則是不要把捲動傳給外層 —— 外層 `overflow:hidden`
   捲不動，傳出去只會在 iOS 上出現整頁被拖的橡皮筋。

⚠️ 下緣的淡出從 **78%** 就開始（不是 90%），讓右下那組小字有比較暗的底。

⚠️ 四周的小字（SAFER HOMES BRIGHTER TOMORROWS、科技守護／從家開始、
   A SAFER LIFE AT HOME）是 **DOM 文字**（`.app__mark`），絕對定位疊在框上面、
   不佔高度、也**不跟著圖滑**。
   ⚠️ 它們的 `text-shadow` 要**厚**（四層暗影）。圖會滑，字底下是亮牆還是暗天空
      是會變的，不能靠固定位置壓暗；單層淡陰影滑到亮牆就直接消失。

⚠️ 這張圖**不透明**，靠它自己的深藍底跟頁面接（頁面底色 `#071c38→#051327→#020a16`，
   圖的四角是 `#012149`／`#012E60`／`#001733` 這一帶，同一個色系）。

#### 另一條路：從 BIM 模型直接算線稿（目前沒有在用）

`tools/` 裡留著一組工具，可以把「一層切平面」那顆 BIM 模型算成軸測線稿：

| 步 | 做什麼 |
| --- | --- |
| `node tools/render-residence.mjs > segs.json` | 解析 gltf、抽**特徵邊**、軸測投影，輸出線段清單 |
| `powershell -File tools/draw-residence.ps1 -Segs segs.json -Width 1200` | 用 System.Drawing 把線段畫成發光線稿 PNG（透明底） |

⚠️ 這條路**目前沒有接到頁面上**，主視覺用的是上面那張圖。工具留著是因為它會動、
   而且是唯一能從真模型產生主視覺的方法。要切回去就把 `.app__pic` 的 `background`
   指回 `residence.png`、`aspect-ratio` 改成圖的比例。

⚠️ 只留**特徵邊**（只被一個三角形用到的邊，或兩面法線夾角 > 18° 的邊）。
   全部三角形邊都畫會變成一團三角網，看不出建築線條。7323 條邊是這樣濾出來的。

⚠️ 玻璃欄杆不收 —— 曲面、佔全模型 94% 的三角形，特徵邊會炸出上萬條曲線雜訊。

⚠️ 那顆模型是**單層**（17.8 × 40.5 × 1.9m），算出來是一片樓板、不是一棟樓。
   要整棟的話主展示端的 `models/tower.glb` 是同一個平面疊三層，同一套工具可以重算。

⚠️ `draw-residence.ps1` 存檔要帶 **UTF-8 BOM**。Windows PowerShell 5.1 用 `-File`
   執行時沒有 BOM 會當成 ANSI 讀，檔案裡的中文變亂碼、字串格式化直接報錯。

⚠️ 這一頁跟其他頁一樣**必須塞進一畫面**，整頁不捲（會滑的只有主視覺那張圖）。
   內容幾乎塞滿，加東西或放大字之前先量一次。

⚠️ 量溢出要看**那一段自己的 `scrollHeight` vs `clientHeight`**，不能看 `document.body`
   —— 各版面都是 `flex:1; min-height:0`，高度被容器決定，內容多出來只會無聲地被
   `overflow:hidden` 裁掉，`body.scrollHeight` 永遠等於 `clientHeight`、看起來像沒事。

⚠️ 三張狀態磚和三顆功能都是**寫死的展示值**，沒有資料來源。

#### 向上滑動的轉場

```css
.pad__main{position:relative}
body.pad.is-swipe .pad__alarm{ display:flex;position:absolute;inset:0;z-index:2;
                               animation:swipeOut .58s var(--ease) forwards }
body.pad.is-swipe .pad__app  { animation:swipeIn .58s var(--ease) }
```

⚠️ 兩個版面平常是 `display:none` 互斥的。動畫期間要讓警報頁**留在畫面上**才滑得出去，
   所以 `.is-swipe` 時把它改成 `position:absolute` 疊上去 ——
   不然兩段會上下排成一列，版面會擠壞。

⚠️ **只有「警報 → app」這一個方向要滑**。主展示端每秒補推一次狀態，
   每次都滑一遍會很吵；`swipe()` 用換頁前後的 `data-view` 判斷方向。

⚠️ 換到別的版面時要**立刻把 `.is-swipe` 收掉**。動畫還沒跑完就換頁的話，
   那個 class 會把警報頁強制 display 出來、絕對定位疊在新畫面上面。
   （心跳重推同一頁時 `from === to`，那時候不能收，不然動畫會被打斷。）

### 5.4 前往 X 出口（`notice`）

抬頭 → 出口指示 → 迷你平面圖 → 剩餘距離／已避開火源，四塊由上往下。

| 元素 | 文字 | 字體 | 字級 | 字重 | 字距 | 顏色 |
| --- | --- | --- | --- | --- | --- | --- |
| `.notice__flame` | 三角形 + 火焰 | — | 2.9rem / 52.3px 寬 | — | — | `--red` |
| `.notice__headtext b` | 緊急疏散提示 | Noto Sans TC | 1.5rem / 27.1px | 700 | .16em | `--ink` |
| `.notice__headtext i` | EMERGENCY NOTICE | **Oswald** | .66rem / 11.9px | 400 | .40em | `--ink-3` |
| `.exit__title` | 請前往／已抵達 … 出口 | Noto Sans TC | 2.15rem / 38.8px | 700 | .06em | `--ink` |
| `.exit__title b` | **A／B**（放大的出口代號） | Noto Sans TC | **2.8rem / 50.4px**（1.3em） | 700 | .06em | 狀態色 |
| `.exit__why` | X 出口目前較暢通，已為你更新疏散方向 | Noto Sans TC | .95rem / 17.1px | 400 | .06em | `--ink-2` |
| `.plan__pin--me b` | 您的位置 | Noto Sans TC | .85rem / 15.3px | 500 | .08em | 狀態色 |
| `.plan__pin--goal b` | X 出口 | Noto Sans TC | .85rem / 15.3px | 700 | .08em | `--ink` |
| `.plan__pin--goal em` | 暢通／已抵達（膠囊） | Noto Sans TC | .72rem / 13px | 500 | .10em | `--exit` |
| `.stat__label` | 剩餘距離 | Noto Sans TC | .8rem / 14.4px | 400 | .14em | `--ink-3` |
| `.stat__value b` | **公尺數** | Noto Sans TC | **2.3rem / 41.5px** | 700 | .02em | 狀態色 |
| `.stat__value` | m | Noto Sans TC | 1.05rem / 18.9px | 400 | .06em | `--ink-2` |
| `.stat__ok` | 已避開火源 | Noto Sans TC | 1.18rem / 21.3px | 500 | .10em | `--exit` |

三張卡：`1px` `--brand-line` 框線、`1.15rem` 圓角、底色 `--bg-2` 46%，
外加一層由上而下的狀態色微光（5%）。

⚠️ 通關時 `.exit__title` 開頭那兩個字要換成「已抵達」（`#n-lead`），
   不能只改副標 —— 標題寫「請前往」副標寫「已抵達」是自相矛盾的。

### 5.5 迷你平面圖

**底圖和動線都是真的模型資料**，不是畫出來的示意圖：

| 來源 | 內容 |
| --- | --- |
| 一層切平面的 BIM 模型 | 模型的**正射俯視投影**，406 圈輪廓，分六層 |
| 主展示端 `models/routes.json` | 動線 1～5 的折線，跟大螢幕同一份 |

底圖畫成**藍圖式的線稿**：`--brand-hi` 描邊、不填色，襯在一層藍色漸層底上
（`.plan__box` 的 radial + linear，中間亮、四周暗）。輪廓本來就是牆的兩側，
描邊出來自然是雙線的牆，跟建築平面圖一樣。

| 圖層 | 內容 | 線寬 | 透明度 |
| --- | --- | --- | --- |
| `.pb--slab` | RC 樓板 —— 建物外框，最亮 | 1.3px | .5 |
| `.pb--wall` | 外牆／RC 內部牆／輕隔間牆 | .9px | .42 |
| `.pb--core` | 核心筒（兩個出口都在這） | .9px | .42 |
| `.pb--col` | 柱子 | .9px | .5 |
| `.pb--stair` | 樓梯 | .8px | .3 |
| `.pb--win` | 窗框 | .8px | .34 |

`tools/extract-plan.mjs` 把兩邊壓成 `js/plan-data.js`（約 28KB）。
**Pad 這個 repo 不放模型檔也不載 three.js**，平板不該為了一張小平面圖去下載 16MB 的模型。

俯視投影是這樣抽的：**只留朝上的三角形**（頂面），把相鄰三角形共用的邊成對消掉，
剩下的邊接成封閉輪廓。閉合實體從上面看到的形狀就是它頂面的輪廓，所以是精確的，不是近似。

⚠️ 不要改用凸包。牆、柱是方盒子凸包剛好等於輪廓，但樓板外框是凹的（建物有缺角），
   凸包會把缺角補起來，圖上就多一條橫貫整層的假牆。門窗開口和樓板的洞也會被封掉。

- 整層樓是 17.6m × 40.3m 的直長形，整張**轉 90°** 讓長邊變橫的（純旋轉，左右沒有鏡射
  —— 鏡射會害住戶把左右走反）
- **五條動線共用同一個裁切框**：整層樓 + 四周留 1m，再撐成 2.25 的長寬比
  （`-1.96 -1.00 44.21 19.65`）

⚠️ 裁切框**不要**改回「照每條動線的範圍各裁一塊」。那樣每條動線的比例尺都不一樣，
   換一條就整張圖忽大忽小、同一道牆忽粗忽細。固定成一個框，五條看起來才是
   同一張圖上的五條路。

#### 動線的顏色和線型

| 部分 | 線型 | 顏色 |
| --- | --- | --- |
| **走過的** | 實線 3.2px ＋ 11px 的光暈（.18） | `--route` |
| **還沒走的** | 虛線 2.4px（`5 5`，.5） | `--route` |
| 起點／終點的點 | 實心圓 | `--route` |
| 小人 | 實心 | **白色**（`#fff`） |

`--route` 是動線專用的色：跑動線時 `--red`，通關之後 `--exit` 綠。
**跟整頁的 `--tone`（藍）分開** —— 大螢幕跑動線時整個介面也是轉紅的，這裡是同一個語彙。

⚠️ 小人**不要**跟著 `--route` 用紅色：它正踩在紅線上，同色就融進去看不到人了。
   白色在藍圖和紅線上都跳得出來。

⚠️ 兩段是 js 依小人的位置**各自算出兩條 path**，不是同一條線疊 `dasharray` 蓋掉 ——
   兩條線的邊緣會露出鋸齒。

⚠️ 兩頭都要判「長度是不是 0」，不是「有沒有點」。走到底時剩下的那段只剩終點一個點、
   跟小人腳下重疊，湊成零長度的線，`stroke-linecap:round` 會在出口上多畫一顆圓點。

#### 動線上的小人

平面圖上有一個小人，位置跟大螢幕那個小人**一模一樣**（對時見 §5.1b）。

每條動線在住戶這一層走完之後還要**往下走樓梯**，`plan-data.js` 兩個長度都留著：

| 欄位 | 是什麼 | 佔全程 |
| --- | --- | --- |
| `floor` | 這一層那一段（平面圖畫得出來的） | 41～59% |
| `total` | 含樓梯的全長 | 100% |

**只取在這一層的那段時間**：俯視圖沒有垂直動線，樓梯間那一段本來就沒東西可看。
所以位置是 `min(tau × total, floor)` —— 小人照真實速度沿走廊走，
在大螢幕的人踏進樓梯間的**同一刻**抵達樓層出口，之後站在出口等
（`.plan__walker[data-arrived="1"]`，輕輕呼吸，讓人看得出是「到了在等」不是「卡住」）。

| 動線 | 這一層 | 含樓梯 | 小人在第幾 % 抵達出口 |
| --- | --- | --- | --- |
| 01 | 20.25m | 41.57m | 48.7% |
| 02 | 19.91m | 41.22m | 48.3% |
| 03 | 14.87m | 36.18m | 41.1% |
| 04 | 24.33m | 42.96m | 56.6% |
| 05 | 26.97m | 45.59m | 59.2% |

⚠️ **不要**改成 `tau × floor`（讓小人整段跑完才到出口）。那樣兩個人的**位置會對不上**：
   大螢幕的人已經在下樓梯了，Pad 的人還在走廊上晃。寧可讓小人提早站定，
   也不要顯示一個和大螢幕不同步的位置。

⚠️ 小人**不要加 transition**。位置本來就是每一格算好的，再補間就變成慢半拍，
   跟大螢幕對不起來。

- 剩餘距離會**跟著小人減少**（`floor` 減掉已經走的），不是固定值 —— 標籤寫「剩餘」就要是剩餘
- 小人一開始走，起點那顆的標籤就從「您的位置」換成「起點」（人已經不在那裡了）

⚠️ 裁切框的長寬比（`js/plan.js` 的 `ASPECT`）要跟 `.plan__box` 的 `aspect-ratio` 一致。
   不一致的話 SVG 會在盒子裡留邊，「您的位置／X 出口」那兩個用百分比疊上去的標籤
   就會對不到圖上的點。

⚠️ 牆和動線的線寬都用 `vector-effect:non-scaling-stroke` —— 每條動線的縮放倍率不同，
   不這樣做的話粗細會忽粗忽細。

---

## 6. 快捷鍵

| 鍵 | 去哪 |
| --- | --- |
| `A` | 前言頁 |
| `Esc` | 火災黃金30秒 |
| `Enter` | 首頁 |
| `O` | 結語頁 |
| `B` | 回藍色介面（不在首頁就先進首頁） |
| `R` | 重置倒數（只在黃金30秒那頁） |
| `E` | 編輯模式（拖曳／縮放版面，存進 `layout*.json`） |

---

## 7. 這份文件怎麼更新

不要手改。改完 UI 之後在瀏覽器 console 跑一次量測（對每個含文字的元素讀 `getComputedStyle`，
記下 `fontFamily` / `fontSize` / `fontWeight` / `letterSpacing` / `color`），再對照更新這裡的表格。
**字級一律以 `rem` 為準**，px 只是 1920×1080 的換算，而且還要再乘上 §0.3 的物件 scale。

## 前往 X 出口頁：最下面那兩顆遙控按鈕

| 元素 | 是什麼 | 尺寸 |
| --- | --- | --- |
| `.nacts` | 統計卡（`.ncard--stats`）裡的**第三格** | `flex:none`、`padding-left:1.1rem`、左邊一條分隔線 |
| `.nact` | ▶ 展示 ／ ⇄ 切換 | 2.2rem × 2.2rem，圖示 1.15rem |

統計卡的順序是 `剩餘距離 → 已避開火源 → ▶ ⇄`，按鈕在整頁的右下角。

⚠️ **不要把它們拉出來自己排一列。** 這一頁（`.pad__notice`）四張卡加起來之後
   只剩 **1.53rem** 的垂直餘裕（實測 375×812：可用 437px、內容 420px），
   而一列按鈕要 2.2rem ＋ 一個 1.35rem 的 gap ≈ 3.5rem —— 會滿出去約 22px。
   更麻煩的是區塊是 `flex:1; min-height:0`，**滿出去是靜靜被裁掉、不會有捲軸**，
   改完當下看起來很正常，實際上底下被切了。
   統計卡本身有 7rem 高，塞進去不佔任何額外高度，這才是它放在那裡的原因。

⚠️ 要驗證有沒有滿出去，量 `section.scrollHeight - section.clientHeight`
   （不是 `document.body.scrollHeight`—— body 量不出來，因為區塊自己會裁）。
   現在這一版是 0。

⚠️ `.nacts` 要**自己補 `border-left`**。分隔線原本是 `.stat + .stat` 這個相鄰選擇器畫的，
   而這一格不是 `.stat`，吃不到。
   ⚠️ 而且它一定要是**最後一格**。放在兩個 `.stat` 中間的話，那兩格就不再相鄰，
      原本 `剩餘距離 | 已避開火源` 之間的分隔線會消失（第一版就是這樣，已修正）。

⚠️ 邊框和圖示用 `--route` 不是 `--tone` —— 跑動線時是紅的、通關之後跟著變綠，
   跟同一頁的動線同色系。

⚠️ **只放 icon 是使用者要求的**，但文字要留在 `title` 和 `aria-label` 裡，不要拿掉。
   沒有標籤的話，這兩顆對讀螢幕的人、和第一次上手的人都是謎。

⚠️ 播放三角形要 `fill:currentColor; stroke:none`（`.nact__solid`）。
   `.nact svg` 預設是只描邊的，三角形只描邊會看起來像個空殼。

⚠️ App 頁那一排（`.app__acts`）只有「逃生指引」是真的 `<button>`，另外兩顆是 `<div>`。
   所以 `.aact` 要帶按鈕的樣式重設（`border:0; background:none; font-family:inherit` …）
   —— 少了 `font-family:inherit` 按鈕會掉回瀏覽器預設字型，三顆就不齊。
